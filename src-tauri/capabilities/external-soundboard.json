{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "external-soundboard-window",
  "description": "Listen-only event access for the external soundboard overlay",
  "windows": ["external-soundboard"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging"
  ]
}
