-- The default Bakbak server and channels are inserted by a migration so hosted
-- environments receive the same data as local development.
--
-- This seed is deliberately credential-free. Never seed an auth password,
-- plaintext invite code, Supabase secret key, or LiveKit API secret here.

